/** Order chat removed — component kept so old imports do not crash builds. */
export function OrderChat(_props: {
  orderId: string;
  authorName: string;
  mode: "customer" | "chef";
  className?: string;
}) {
  return null;
}
